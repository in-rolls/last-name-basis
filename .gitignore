__pycache__/
*.pyc
.venv/
.pytest_cache/

# Generated tables are rebuildable from the pipelines; figures are tracked so
# the notes render on GitHub.
out/tab/
analyses/*/out/tab/
# Analysis 04's scan reads ~1.5GB of rolls and takes ~8 minutes. Its two
# small tables are tracked so a stray delete does not cost that again.
!analyses/04_which_token_is_the_surname/out/tab/
!analyses/04_which_token_is_the_surname/out/tab/*.csv
*.egg-info/

# The quiz log is per-deployment, not part of the repo.
app/data/rounds.csv

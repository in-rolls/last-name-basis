[flake8]
max-line-length = 88
extend-ignore = E203
# note.py is mostly a markdown template; wrapping prose to satisfy a Python
# line limit makes the output worse, not the code better.
per-file-ignores =
    analyses/*/note.py: E501
